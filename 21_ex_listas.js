const listaDeNumeros = [34, 56, 768, 8, 810, 3, 6, 210];

// 1. Qual a soma dos números da lista?
let soma = 0;

// para (cada elemento da lista)
for (const numeros of listaDeNumeros) {
    // some os elementos
    soma += numeros; 
}
// mostre o resultado da soma dos elementos
console.log(soma);

// 2. Qual o menor número da lista?