//array são listas

let lista = ['banana', 'maçã', 'açúcar', 'beterraba'];
console.log(lista);
// a const frutas, vai ser o elemento que o of do for, percorre dentro de uma Array(lista)
for (const frutas of lista) { 
    console.log(frutas);
}